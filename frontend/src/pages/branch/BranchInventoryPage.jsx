import React, { useMemo, useState } from 'react';
import { AlertTriangle, ChevronDown, Package, Search } from 'lucide-react';
import { BranchCard, BranchShell, BranchStatus } from './BranchShell';

const inventory = [['Shelf A1', 'Inbound parcels', '128', 'Healthy', 'teal'], ['Shelf A2', 'Ready for dispatch', '96', 'Healthy', 'teal'], ['Cage B1', 'Held / exception', '12', 'Review', 'amber'], ['Rack C3', 'Oversize parcels', '8', 'Capacity warning', 'coral']];
export default function BranchInventoryPage() { const [query, setQuery] = useState(''); const [condition, setCondition] = useState('All conditions'); const visible = useMemo(() => inventory.filter((row) => (!query || row.join(' ').toLowerCase().includes(query.toLowerCase())) && (condition === 'All conditions' || row[3] === condition)), [condition, query]); return <BranchShell title="Inventory" subtitle="Monitor package locations, capacity, and branch exceptions."><div className="branch-kpis"><Kpi label="Total packages" value="244" detail="Across 4 locations" /><Kpi label="Available capacity" value="68%" detail="Within safe limit" /><Kpi label="Held packages" value="12" detail="Requires review" /><Kpi label="Scans today" value="428" detail="Last scan 09:32" /></div><BranchCard><div className="branch-heading"><div><span className="branch-eyebrow">Package locations</span><h2>Branch inventory</h2></div></div><div className="branch-toolbar"><label className="branch-search"><Search size={15} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search shelf or package type" /></label><label className="branch-select"><select value={condition} onChange={(event) => setCondition(event.target.value)}><option>All conditions</option><option>Healthy</option><option>Review</option><option>Capacity warning</option></select><ChevronDown size={13} /></label></div><table className="branch-table"><thead><tr><th>Location</th><th>Contents</th><th>Packages</th><th>Condition</th></tr></thead><tbody>{visible.map((row) => <tr key={row[0]}><td><Package size={14} /> {row[0]}</td><td>{row[1]}</td><td><strong>{row[2]}</strong></td><td><BranchStatus tone={row[4]}>{row[3]}</BranchStatus></td></tr>)}</tbody></table>{visible.some((row) => row[4] === 'coral') && <div className="branch-alert"><AlertTriangle size={15} /> Rack C3 is approaching its safe capacity limit.</div>}</BranchCard></BranchShell>; }
function Kpi({ label, value, detail }) { return <div className="branch-kpi"><span>{label}</span><strong>{value}</strong><small>{detail}</small></div>; }
