import React from 'react';

const styles = `.common-table-shell{width:100%;overflow-x:auto;border:1px solid #E3E7EF;border-radius:14px;background:#fff}.common-table{width:100%;border-collapse:collapse;min-width:560px}.common-table th{padding:12px 14px;color:#9AA1B4;font-size:10.5px;font-weight:700;letter-spacing:.05em;text-align:left;text-transform:uppercase;background:#F9FAFC}.common-table td{padding:13px 14px;border-top:1px solid #E3E7EF;color:#12213F;font-size:12.5px;vertical-align:middle}.common-table tbody tr:hover td{background:#FAFBFD}.common-table-empty{padding:28px 18px;color:#697086;text-align:center;font-size:12px}.common-table-loading{display:flex;align-items:center;justify-content:center;gap:8px;padding:28px;color:#697086;font-size:12px}.common-table-spinner{animation:common-table-spin 1s linear infinite}@keyframes common-table-spin{to{transform:rotate(360deg)}}`;

export default function Table({ columns = [], data = [], rowKey = 'id', loading = false, emptyMessage = 'No records found.', renderRow, className = '' }) {
	return <div className={`common-table-shell ${className}`.trim()}><style>{styles}</style>{loading ? <div className="common-table-loading"><span className="common-table-spinner">◌</span>Loading records...</div> : data.length === 0 ? <div className="common-table-empty">{emptyMessage}</div> : <table className="common-table"><thead><tr>{columns.map((column) => <th key={column.key || column.label}>{column.label}</th>)}</tr></thead><tbody>{data.map((row, index) => renderRow ? renderRow(row, index) : <tr key={row[rowKey] ?? index}>{columns.map((column) => <td key={column.key || column.label}>{column.render ? column.render(row, index) : row[column.key]}</td>)}</tr>)}</tbody></table>}</div>;
}
