import React from 'react';

const styles = `.driver-performance-chart{padding:18px 20px;border:1px solid #E3E7EF;border-radius:14px;background:#fff}.driver-performance-title{margin:0 0 14px;color:#12213F;font:700 14.5px 'Sora',sans-serif}.driver-chart{width:100%;height:180px}.driver-chart-labels{display:flex;justify-content:space-between;color:#9AA1B4;font-size:10px}`;

export default function DriverPerformanceChart({ data = [], title = 'Driver performance' }) { const values = data.length ? data : [62, 78, 71, 84, 76, 92, 88]; const max = Math.max(...values, 1); const points = values.map((value,index)=>`${(index/Math.max(values.length-1,1))*100},${92-(value/max)*76}`).join(' '); return <div className="driver-performance-chart"><style>{styles}</style><h2 className="driver-performance-title">{title}</h2><svg className="driver-chart" viewBox="0 0 100 100" preserveAspectRatio="none" role="img" aria-label={title}><path d="M0 92H100 M0 54H100 M0 16H100" stroke="#EEF1F6" strokeWidth=".8" fill="none" vectorEffect="non-scaling-stroke"/><polyline points={points} fill="none" stroke="#3E7BFA" strokeWidth="2.5" vectorEffect="non-scaling-stroke" strokeLinecap="round" strokeLinejoin="round"/></svg><div className="driver-chart-labels">{values.map((_,index)=><span key={index}>D{index+1}</span>)}</div></div>; }
